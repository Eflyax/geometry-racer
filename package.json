{
	"name": "animal-racer",
	"version": "1.0.0",
	"private": true,
	"workspaces": [
		"shared",
		"server",
		"client"
	],
	"scripts": {
		"build:shared": "npm run build --workspace=shared",
		"dev:server": "npm run dev --workspace=server",
		"dev:client": "npm run build --workspace=shared && npm run dev --workspace=client",
		"test:e2e": "npx playwright test"
	},
	"devDependencies": {
		"@playwright/test": "^1.59.1"
	},
	"dependencies": {
		"tsc": "^2.0.4",
		"typescript": "^6.0.3"
	}
}
